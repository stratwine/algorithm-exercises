/*
package com.algo.tree;

class MinTreeBuilder {


    public static void main(String[] args) {

        Integer []arr= new Integer[]{1,2,3,4,5,6,7,8};
        Integer midPosition = arr[arr.length/2];

        TreeNode node = new TreeNode(arr[midPosition]);

        buildTree(node,0,midPosition-1);
        buildTree(node,midPosition+1,arr.length-1);
    }

    private static TreeNode buildTree(TreeNode node, int start, int end) {

        node.data=
    }

}*/
